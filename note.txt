- Debugged authenticate middleware
- Created a function to handle cookies
- Added default usernames
- included user information in profile page
- handled pfp uploads (uploading/deleting pfps)
- added rls to supabase storage


Error Handlers to set
  username already exists (profile page)
  Connection timeout (slow network)
  weak password (login/signup page)


Functionalities to set
  Logged in users can't visit login/signup page(redirect them)

To test
  created_at column in userPosts